{% extends 'base.html.twig' %}

{% form_theme form "bootstrap_3_horizontal_layout.html.twig"%}

{% block body %}
    <h1>Novo Produto</h1>

    {{ form_start(form, {'attr':{'novalidate':'novalidate'} } ) }}
        {{ form_widget(form) }}
        <input type="submit" value="Criar" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('produtos_index') }}">Voltar para a lista</a>
        </li>
    </ul>
{% endblock %}


